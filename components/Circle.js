import React from 'react'

const Circle = ({ top, left, right, bottom, backgroundColor }) => {
    return (
        <div style={{ top, left, right, bottom, backgroundColor }} className="circle">

        </div>
    )
}

export default Circle